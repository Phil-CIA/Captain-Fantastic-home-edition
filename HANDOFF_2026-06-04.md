# Captain-Fantastic Handoff - 2026-06-04

## Session Outcome

Session pivoted from recap into controlled LED/runtime A/B testing and then into GPIO15 strip routine expansion.

### Confirmed outcomes
1. Matrix periodic diagnostics were converted to explicit toggles and tested in single-variable and all-off combinations.
2. Best visual matrix lamp state today remained with all three periodic features off.
3. Control heartbeat on GPIO15 was fully disabled by policy (all modes off) and hardened with a feature gate.
4. Gameplay-driven GPIO15 WS2812 effects were added (non-blocking), then expanded from single-pixel to zone-mapped strip behavior with current limiting.
5. Switch recognition concern remains active; no hard failure observed, but user reported it may feel worse than before and asked to monitor while proceeding.

## Current Firmware State (As Left)

### Matrix (Captain-v2-matrix)
File: Captain-v2-matrix/src/matrix_app_main.cpp

- MATRIX_ENABLE_STATUS_HEARTBEAT = false
- MATRIX_ENABLE_LINK_LOG = false
- MATRIX_ENABLE_PERIODIC_IDLE_YIELD = false

These toggles are in the main loop and currently all disabled.

### Control (Captain-v2)
File: Captain-v2/src/control_main.cpp

- GPIO15 WS2812 chain length set to 301 pixels (D1 + external strip chain assumption)
- Heartbeat policy set OFF in all gameplay modes
- HEARTBEAT_FEATURE_ENABLED gate added so heartbeat is disabled at init when all mode flags are false
- Gameplay pulse engine added (non-blocking)
- Zone mapping added:
  - 6 zones
  - 20 pixels each
  - start at pixel 1 (strip-focused)
- Current limiting added:
  - channel clamp: GAMEPLAY_LED_MAX_CHANNEL = 40
  - estimated current budget: GAMEPLAY_LED_CURRENT_LIMIT_MA = 250
- Switch-to-zone mapping currently uses row/col index modulo zone count

### Bench observation from user
- Strip showed visible activity around first ~20 LEDs (not full strip)
- Gameplay behavior felt unchanged
- Switch recognition concern persisted (possibly worse, not confirmed)

## Safety / Flashing

Guarded flashing remained enforced throughout:
1. COM12 remained blocked.
2. Matrix uploads verified COM4 MAC target.
3. Control uploads verified COM5 MAC target.

## Monitoring Added

New script created: scripts/Monitor-SwitchHealth.ps1

Purpose:
- Watches control serial "Matrix link" summaries and highlights potential regression signals while LED work proceeds.

Default monitored fields:
- ready/fault
- wr_fail/rd_fail
- sup_le
- sw_edges

Example usage:
- powershell -ExecutionPolicy Bypass -File scripts/Monitor-SwitchHealth.ps1 -Port COM5 -DurationSeconds 120

Notes:
- Threshold for sup_le warning was tuned higher to reduce false positives.

## Files Changed This Session (Primary)

- Captain-v2-matrix/src/matrix_app_main.cpp
- Captain-v2/src/control_main.cpp
- scripts/Monitor-SwitchHealth.ps1 (new)

Other repo state at close also includes existing unrelated local changes and untracked files.

## User-Requested Direction Change

User requested closeout and reset plan because the path drifted from desired approach.

New direction:
1. Mine handoffs/docs/code history first.
2. Confirm whether LED routines from 2026-05-10 to 2026-05-15 already define intended zone/current-limit approach.
3. Check if relevant routines/history may be in another folder or repo.
4. If archaeology finds nothing decisive, restart strip routine design cleanly (similar to today but with stricter spec-first mapping).

## Reset Plan For Next Session

### Phase A - Archaeology First (No new behavior changes)
1. Re-read handoffs:
   - HANDOFF_2026-06-03.md
   - END_OF_DAY_HANDOFF_2026-05-10.rmd
   - END_OF_DAY_HANDOFF_2026-05-12.md
   - END_OF_DAY_HANDOFF_2026-05-13.md
   - END_OF_DAY_HANDOFF_2026-05-14.md
   - END_OF_DAY_HANDOFF_2026-05-15.md
2. Mine git history 2026-05-10..2026-05-15 for LED/switch routines in:
   - Captain-v2/src/control_main.cpp
   - Captain-v2-matrix/src/matrix_app_main.cpp
3. Expand search to likely adjacent folders in this repo that may have parallel implementation attempts:
   - Captain-v2-control/
   - display-firmware/
   - src/
   - include/
4. Cross-check second workspace repo for possible mixed implementation notes/routines:
   - WorkStation/

### Phase B - Decision Gate
1. If prior routines already match desired architecture, restore/adapt minimally.
2. If archaeology does not reveal suitable routines, start clean strip spec:
   - explicit segment map
   - explicit current budget
   - event-to-zone mapping table
   - switch regression watch required during each flash/test cycle

### Phase C - Regression Guard
1. Run Monitor-SwitchHealth.ps1 during every LED test pass.
2. Stop and investigate if ready/fault or fail counters regress materially.

## Suggested First Commands Next Session

- git -C "C:\Users\user\Esp32 projects VScode\Captain-Fantastic-home-edition" log --since="2026-05-10" --until="2026-05-16" --name-only -- Captain-v2/src/control_main.cpp Captain-v2-matrix/src/matrix_app_main.cpp
- powershell -ExecutionPolicy Bypass -File scripts/Monitor-SwitchHealth.ps1 -Port COM5 -DurationSeconds 120

## Open Questions

1. Is the external strip electrically/data-wise healthy beyond first ~20 LEDs (power injection, ground integrity, data integrity)?
2. Is switch recognition degradation real regression or workload/feel variance?
3. Should the zone map be physical-playfield mapped (lanes/targets/returns) rather than modulo-based mapping?
