# Captain-Fantastic-home-edition Handoff - May 24, 2026

## Mission Snapshot

This session was spent re-establishing a trustworthy A/B baseline for the controlled-lamp flicker investigation.

The state now is:
- COM4 has been reflashed with the known-good matrix baseline again
- COM4 restore succeeded only after using the no-space mirror at `C:\_tmp\captain_v2_matrix`
- COM5 has been reflashed with the current alternating read/write control build
- the next useful work is bench observation, not more environment repair unless flashing breaks again

## Read This First

1. `END_OF_DAY_HANDOFF_2026-05-24.md`
2. `NEXT_CHAT_PROMPT_2026-05-24.txt`
3. `Captain-v2/src/control_main.cpp`
4. `Captain-v2-matrix/src/matrix_app_main.cpp`
5. `/memories/repo/pinball-matrix-bringup-notes.md`

## Current State

### Good
- COM4 `captain_matrix_idf` upload succeeded
- COM5 `captain_control` upload succeeded
- matrix and control boards are back in the intended comparison state
- current control firmware still contains the alternating read/write isolation build

### Important context
- COM4 should now be left alone during the next diagnostic step
- the matrix project is still path-sensitive and behaved best from `C:\_tmp\captain_v2_matrix`
- local PlatformIO platform state was damaged and had to be repaired outside the repo

## Immediate Next Task

Do not start by changing firmware again.

Next session should:
- power up the machine with the current flashed pair
- observe lamp flicker and switch response on the real machine
- decide from those observations whether the alternating COM5 build helps enough to keep investigating transaction cadence or whether to step to a different control-side probe

## Constraints

1. Treat COM4 as the frozen known-good matrix baseline.
2. Do not broaden back into random matrix-side edits before observing the freshly restored baseline pair.
3. If matrix flashing is needed again, prefer the `C:\_tmp\captain_v2_matrix` mirror path.
4. Treat `Captain-v2/src/control_main.cpp` as the source of truth for the current COM5 test build.

## Quick Commands

- Matrix upload from mirror: `Set-Location C:\_tmp\captain_v2_matrix; platformio run -e captain_matrix_idf -t upload --upload-port COM4`
- Control build: `Set-Location Captain-v2; platformio run -e captain_control`
- Control upload: `Set-Location Captain-v2; platformio run -e captain_control -t upload --upload-port COM5`

## Status

Current State: The session is closed with COM4 restored to the known-good matrix baseline and COM5 flashed with the alternating control-side read/write test build.

Last Update: May 24, 2026