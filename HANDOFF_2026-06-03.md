# Captain-Fantastic-home-edition Handoff — 2026-06-03

## Session Outcome

Baseline recovery and safety standardization completed for split-MPU firmware:
- Matrix board recovered and flashed through guarded workflow on COM4.
- Control board recovered and flashed through guarded workflow on COM5.
- CrowPanel COM12 remained blocked throughout.
- Firmware identity stamping is now part of the standard (control boot prints build identity).

WS2812 strip state confirmed:
- GPIO15 strip path is live and verified on COM5.
- Control boot prints `Heartbeat enabled on GPIO15 (WS2812)`.
- The first pixel on the strip is responding from the same GPIO15 / TP8 net.
- Matrix RGB/status code remains a separate path and is not the focus for the next session.

## Standards Now In Effect

1. Guarded flash is mandatory for matrix/control uploads.
2. MAC identity is the primary guardrail.
3. COM12 is hard-blocked in this session workflow.
4. MAC mismatch requires explicit operator override (`-ForceMacMismatch`).
5. Control firmware must print build identity on boot (`git`, `state`, build timestamp).

## Key Files Added/Changed This Session

- Added: `scripts/guarded-flash.ps1`
- Added: `scripts/flash-targets.json`
- Added: `scripts/pio_build_meta.py`
- Modified: `.vscode/tasks.json`
- Modified: `Captain-v2/platformio.ini`
- Modified: `Captain-v2/src/control_main.cpp`
- Modified: `Captain-v2-matrix/platformio.ini`
- Modified: `HANDOFF_2026-05-15.md`

## Verified Hardware Identity (Current)

- Matrix target (COM4): `10:51:DB:FF:FE:0D:30:30`
- Control target (COM5): `44:1D:64:F8:2B:A0`

Source of truth: `scripts/flash-targets.json`.

## Known Good Commands

- Matrix build: `powershell -ExecutionPolicy Bypass -File scripts/guarded-flash.ps1 -Target matrix -Action build`
- Matrix upload: `powershell -ExecutionPolicy Bypass -File scripts/guarded-flash.ps1 -Target matrix -Action upload`
- Control build: `powershell -ExecutionPolicy Bypass -File scripts/guarded-flash.ps1 -Target control -Action build`
- Control upload: `powershell -ExecutionPolicy Bypass -File scripts/guarded-flash.ps1 -Target control -Action upload`

## Version Identity Behavior

Control setup now prints a line like:
- `Build: git=<short-hash> state=<clean|dirty> built=<date> <time>`

Implemented in:
- `Captain-v2/src/control_main.cpp`
- `scripts/pio_build_meta.py`

## Current Git State Snapshot

At handoff capture:
- HEAD: `7e1a312`
- Working tree includes modifications related to guard + version stamping, plus existing user changes in this branch.

## Tree Cleanup Completed (Archive-First, No Hard Deletes)

Archived artifacts were moved to:
- `archive/session-artifacts/2026-06-03/matrix-bak/`
- `archive/session-artifacts/2026-06-03/captain-v2-captures/`

Moved files:

1. Matrix backup artifacts
- `Captain-v2-matrix/src/matrix_app_main.cpp.bak-2026-05-05`
- `Captain-v2-matrix/src/matrix_app_main.cpp.bak-2026-05-12`
- `Captain-v2-matrix/src/matrix_app_main.cpp.bak-2026-05-13`

2. Captain-v2 capture artifacts
- `Captain-v2/capture.txt`
- `Captain-v2/serial_capture.txt`
- `Captain-v2/map_capture_short.txt`
- `Captain-v2/map_capture_sync.txt`
- `Captain-v2/map_capture_sync_smoketest.txt`
- `Captain-v2/map_capture_after_filter_fix.txt`

Notes:
- `pio_build_check.log` and `pio_upload_check.log` were not present at cleanup time.
- Build output directories remain intentionally untouched (`.pio/`, `Captain-v2/.pio/`) and can be purged later for disk cleanup if desired.

## Suggested Next Session Start

1. Resume from the 5/12 to 5/15 handoff set and compare the LED-related code changes directly.
2. Keep the scope on the WS2812 strip / LED restore path first, then decide whether any matrix RGB/status work is still needed.
3. Verify the current control boot output still reports the WS2812 GPIO15 line and build identity stamp.
4. Optionally purge generated `.pio/` directories if disk cleanup is needed.

## Safety Constraints

- Do not flash COM12/CrowPanel in this Captain session flow.
- Do not bypass MAC checks unless explicitly intended and acknowledged.
- Keep matrix and control uploads explicit by target and port.

## Tomorrow's Focus

The next session should use the 2026-05-12 through 2026-05-15 handoff trail to identify exactly which LED-related code changes are worth keeping.

Primary questions for tomorrow:
- What changed between the 5/12 WS2812 restore and the 5/15 flicker triage state?
- Which changes belong to the GPIO15 strip path and which are matrix-only diagnostics?
- Is any further restore actually needed, or is the control WS2812 path already the correct end state?
